import { ComponentProps } from "../../lib/component-props";
import { PromoCardsDefault } from "./PromoCardsDefault.dev";

export const Default = (props: ComponentProps) => {
  return <PromoCardsDefault {...props} />;
};
